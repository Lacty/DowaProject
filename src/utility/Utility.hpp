
#pragma once
#include "cinder/Vector.h"


// turncates the deciminal point [len] or less
ci::Vec2f floorv2(const ci::Vec2f& in, int len);

// turncates the deciminal point [len] or less
double floor2(double in, int len);